from helpers.ui.kalgudi.fisicalyear_class import *

def test_create_fisical_year(browser, url, username, password):
    FY = FYClass(browser)
    FY.login(url,browser, username, password)    
    FY.create_fisicalyear(browser)
    FY.logout(browser)
